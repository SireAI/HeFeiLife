def latestRelease = "latest.release"

//类库版本以及sdk统一配置
Properties dependency = new Properties()
dependency.load(project.rootProject.file('dependency.properties').newDataInputStream())

ext{
    arch_room_version = '1.1.0-beta2'
    dagger_version = "2.14.1"
    crashreport_version = "latest.release"
    mpermission_version = "1.0.0"
    glide_version = "4.1.1"
    compileSdkVersion = 27
    buildToolsVersion = "27.0.3"
    minSdkVersion = 18
    targetSdkVersion = 21
    versionCode = 1
    versionName = "1.0.0"
    mainapplicationId = "com.sire.hefeilife"

    //模块依赖配置
    Mediators = dependency.getProperty("MediatorsLocal").equals("true")?project(':Mediators'):"com.sire:Mediators:$latestRelease"
    ModuleEnvironment = dependency.getProperty("ModuleEnvironmentLocal").equals("true")?project(':ModuleEnvironment'):"com.sire:ModuleEnvironment:$latestRelease"
    BaiduLocationModule = dependency.getProperty("BaiduLocationModuleLocal").equals("true")?project(':BaiduLocationModule'):'com.sire:BaiduLocationModule:1.0.0'
    BBSModule = dependency.getProperty("BBSModuleLocal").equals("true")?project(':BBSModule'):'com.sire:BBSModule:1.0.0'
    FeedModule = dependency.getProperty("FeedModuleLocal").equals("true")?project(':FeedModule'):'com.sire:FeedModule:1.0.0'
    MessagePushModule = dependency.getProperty("MessagePushModuleLocal").equals("true")?project(':MessagePushModule'):'com.sire:MessagePushModule:1.0.0'
    UpgradeModule = dependency.getProperty("UpgradeModuleLocal").equals("true")?project(':UpgradeModule'):'com.sire:UpgradeModule:1.0.0'
    UserModule = dependency.getProperty("UserModuleLocal").equals("true") ?project(':UserModule'):'com.sire:UserModule:1.0.0'
    ShareModule = dependency.get("ShareModuleLocal").equals("true")?project(':ShareModule'):'com.sire:ShareModule:1.0.0'

}
println "========== 模块依赖情况 ============"
println Mediators
println ModuleEnvironment
println BaiduLocationModule
println BBSModule
println FeedModule
println MessagePushModule
println UpgradeModule
println UserModule
println ShareModule